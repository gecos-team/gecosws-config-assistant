# user preferences (managed by chef)

<% @config.each do |cfg| %>
<% if cfg.value.kind_of?(String)  %>
<% valuemod = "'#{cfg[:value]}'" %>
<% else %>
<% valuemod = cfg[:value] %>
<% end %>
user_pref("<%= cfg[:key] %>", <%= valuemod %>);
<% end %>

